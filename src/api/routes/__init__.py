"""
API routes module.

Organizes FastAPI routes by domain (auth, query, conversations, admin, analytics, feedback).
"""

# Route modules are imported individually in api.py
# This allows better control over dependency injection
